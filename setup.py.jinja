from setuptools import find_packages, setup

setup(
    name="{{ project_name }}",
    packages=find_packages(),
    version="{{ version }}",
    description="{{ project_description }}",
    author="{{ author }}",
    license="",
)
